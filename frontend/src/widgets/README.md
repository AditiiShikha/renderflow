# frontend/src/widgets — owned by Person C

The 6 widget components (`gauge`, `trend`, `numeric_card`, `alarm_banner`,
`table`, `toggle`) and `WidgetRegistry` go here. Build against
`/shared/fixtures/screen-spec-example.json` and the prop shapes in
`/shared/schemas/widget.schema.json` before any real API call exists.
